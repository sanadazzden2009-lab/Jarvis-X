def generate_reply(text: str) -> str:
    """
    Placeholder Groq provider.

    No network request yet - this step only proves the providers layer is
    wired correctly end to end. The real Groq API call is added in the
    next step.
    """
    return "Groq provider ready."
